/*
 * VanChip Messages
 *
 * This contains all the text for the VanChip component.
 */
import { defineMessages } from 'react-intl';

const scope = 'app.domains.van.components.VanChip';

export default defineMessages({
  defaultLabel: {
    id: `${scope}.defaultLabel`,
    defaultMessage: 'Default',
  },
});
