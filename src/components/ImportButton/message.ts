import { defineMessages } from 'react-intl';

const scope = 'app.components.ImportButton';

export default defineMessages({
  label: {
    id: `${scope}.label`,
    defaultMessage: 'Import Data',
  },
});
