/*
 * SessionFilter Messages
 *
 * This contains all the text for the SessionFilter component.
 */
import { defineMessages } from 'react-intl';

const scope = 'app.components.SessionFilter';

export default defineMessages({
  sessionLabel: {
    id: `${scope}.sessionLabel`,
    defaultMessage: 'Session',
  },
});
