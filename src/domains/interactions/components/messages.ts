import { defineMessages } from 'react-intl';

const scope = 'app.interactions.components';

export default defineMessages({
  pageTitle: {
    id: `${scope}.pageTitle`,
    defaultMessage: 'Interaction Report',
  },
});
