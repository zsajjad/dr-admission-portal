import { defineMessages } from 'react-intl';

const scope = 'app.components.ExportButton';

export default defineMessages({
  label: {
    id: `${scope}.label`,
    defaultMessage: 'Export Data',
  },
});
