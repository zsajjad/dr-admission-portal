/*
 * ClassLevelGroupFilter Messages
 *
 * This contains all the text for the ClassLevelGroupFilter component.
 */
import { defineMessages } from 'react-intl';

const scope = 'app.components.ClassLevelGroupFilter';

export default defineMessages({
  label: {
    id: `${scope}.label`,
    defaultMessage: 'Class Level Group',
  },
});
