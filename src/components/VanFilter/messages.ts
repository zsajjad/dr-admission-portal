/*
 * VanFilter Messages
 *
 * This contains all the text for the VanFilter component.
 */
import { defineMessages } from 'react-intl';

const scope = 'app.components.VanFilter';

export default defineMessages({
  vanLabel: {
    id: `${scope}.vanLabel`,
    defaultMessage: 'Van',
  },
});
