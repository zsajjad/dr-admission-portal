/*
 * ClassLevelFilter Messages
 *
 * This contains all the text for the ClassLevelFilter component.
 */
import { defineMessages } from 'react-intl';

const scope = 'app.components.ClassLevelFilter';

export default defineMessages({
  classLevelLabel: {
    id: `${scope}.classLevelLabel`,
    defaultMessage: 'Class Level',
  },
});
