/*
 * AreaSelect Messages
 *
 * This contains all the text for the AreaSelect component.
 */
import { defineMessages } from 'react-intl';

const scope = 'app.components.AreaSelect';

export default defineMessages({
  areaLabel: {
    id: `${scope}.areaLabel`,
    defaultMessage: 'Area',
  },
});
