/*
 * BranchSelect Messages
 *
 * This contains all the text for the BranchSelect component.
 */
import { defineMessages } from 'react-intl';

const scope = 'app.components.BranchSelect';

export default defineMessages({
  branchLabel: {
    id: `${scope}.branchLabel`,
    defaultMessage: 'Branch',
  },
});
