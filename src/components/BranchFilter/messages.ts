/*
 * BranchFilter Messages
 *
 * This contains all the text for the BranchFilter component.
 */
import { defineMessages } from 'react-intl';

const scope = 'app.components.BranchFilter';

export default defineMessages({
  branchLabel: {
    id: `${scope}.branchLabel`,
    defaultMessage: 'Branch',
  },
});
