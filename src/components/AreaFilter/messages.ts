/*
 * AreaFilter Messages
 *
 * This contains all the text for the AreaFilter component.
 */
import { defineMessages } from 'react-intl';

const scope = 'app.components.AreaFilter';

export default defineMessages({
  areaLabel: {
    id: `${scope}.areaLabel`,
    defaultMessage: 'Area',
  },
});
